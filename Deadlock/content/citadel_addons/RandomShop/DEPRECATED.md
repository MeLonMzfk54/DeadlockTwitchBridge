# Deprecated — stale snapshot

This folder is an **obsolete copy** of early Random Shop Panorama files.

**Live source of truth:** `Deadlock/content/citadel_addons/twitch_minimap_fx/`

That addon includes:

- `citadel_hud_hero_shop.xml`
- `random_shop.js` / `random_shop.css`
- `twitch_bridge_shop.js` (shop vote HUD + PNG cmd, mod **1.8.0+**)

Do **not** package or enable a separate Random Shop `pak02` alongside the bridge VPK — layout conflicts break voting and safe purchase. See `twitch_minimap_fx/PACKAGING.md` and the repo README (Random Shop / shop vote).

Kept only for historical diff; do not edit here for new features.
